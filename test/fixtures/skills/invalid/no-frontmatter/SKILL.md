# No Frontmatter Skill

This skill file has no YAML frontmatter at all.
It should fail validation because frontmatter is required.

## Purpose

This demonstrates what happens when the --- delimiters are missing.
